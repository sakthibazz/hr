name: Deploy to Remote Server

on:
  push:
    branches:
      - main  # Replace with your desired branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "$AWS_SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 644 ~/.ssh/id_rsa
          ssh-keyscan -H 51.20.78.69 >> ~/.ssh/known_hosts  # Replace <REMOTE_HOST> with the actual host
          chmod 644 ~/.ssh/known_hosts

      - name: Deploy to Remote Server
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ubuntu@51.20.78.69  /home/ubuntu/deploy-script.sh  # Replace with actual remote user, host, and script path
